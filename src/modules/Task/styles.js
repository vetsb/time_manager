const styles = theme => ({
	buttons: {
		...theme.lineButtons,
		marginTop: 20,
	},
	button: theme.lineButton,

	inner: theme.inner,
	container: theme.container,

	errorTitle: theme.errorTitle,
});

export default styles;