const styles = theme => ({
	inner: theme.inner,
	container: theme.container,

	errorTitle: theme.errorTitle,

	title: {
		display: 'flex',
		alignItems: 'center',
	},
	titleSpan: {
		marginRight: 25,
	}
});

export default styles;